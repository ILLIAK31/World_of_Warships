#include <iostream>
#include <vector>
#include <string>

using namespace std;

#include "Ship.hpp"

Ship::Ship(string color, string rotation,string symbol):color(color),rotation(rotation),symbol(symbol){}

Ship::~Ship(){}